define([
    'jquery', 'underscore', 'backbone'],
    function module($, _, Backbone) {

    var app = _.extend({

        start: function start() {
            
        }

    });

    return app;
});
