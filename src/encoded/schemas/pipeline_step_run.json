{
    "title": "Pipeline step run",
    "description": "Schema for reporting a specific instance of a pipeline step execution.",
    "id": "/profiles/pipeline_step_run.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "required": ["pipeline_run", "analysis_step_version"],
    "additionalProperties": false,
    "identifyingProperties": ["uuid"],
    "mixinProperties": [
        { "$ref": "mixins.json#/schema_version" },
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/notes" },
        { "$ref": "mixins.json#/standard_status" }
    ],
    "properties": {
        "schema_version": {
            "default": "1"
        },
        "pipeline_run": {
            "description": "Reference to the pipeline run this step execution is a part of.",
            "comment": "See pipeline_run.json for available identifiers.",
            "type": "string",
            "linkTo": "PipelineRun"
        },
        "analysis_step_version": {
            "title": "Analysis Step Version",
            "description": "Reference to the analysis step version this pipeline step run is an instantiation of.",
            "type": "string",
            "linkTo": "AnalysisStepVersion"
        },
        "inputs": {
            "title": "Input file blobs",
            "description": "File blobs used as input for this pipeline step run.",
            "type": "array",
            "default": [],
            "uniqueItems": true,
            "items": {
                "title": "File blob",
                "description": "A particular file blob specified as input for this pipeline step run.",
                "type": "string",
                "linkTo": "FileBlob"
            }
        },
        "outputs": {
            "title": "Output file blobs",
            "description": "File blobs that were produced as output of this pipeline step run.",
            "type": "array",
            "default": [],
            "uniqueItems": true,
            "items": {
                "title": "File blob",
                "description": "A particular file blob produced as an output of this pipeline step run.",
                "type": "string",
                "linkTo": "FileBlob"
            }
        }
    },
    "facets": {
        "status": {
            "title": "Status"
        }
    },
    "changelog": "/profiles/changelogs/pipeline_step_run.md"
}