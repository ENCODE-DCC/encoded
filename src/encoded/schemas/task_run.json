{
    "title": "Task run",
    "description": "Schema for reporting a specific instance of a workflow task execution.",
    "id": "/profiles/task_run.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "required": ["pipeline_run", "analysis_step_version"],
    "additionalProperties": false,
    "identifyingProperties": ["uuid"],
    "mixinProperties": [
        { "$ref": "mixins.json#/schema_version" },
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/notes" },
        { "$ref": "mixins.json#/standard_status" }
    ],
    "properties": {
        "schema_version": {
            "default": "1"
        },
        "workflow_run": {
            "description": "Reference to the workflow run this task execution is a part of.",
            "comment": "See workflow_run.json for available identifiers.",
            "type": "string",
            "linkTo": "WorkflowRun"
        },
        "analysis_step_version": {
            "title": "Analysis Step Version",
            "description": "Reference to the analysis step version this task run is an instantiation of.",
            "type": "string",
            "linkTo": "AnalysisStepVersion"
        },
        "inputs": {
            "title": "Input file blobs",
            "description": "File blobs used as input for this workflow task run.",
            "type": "array",
            "default": [],
            "uniqueItems": true,
            "items": {
                "title": "File blob",
                "description": "A particular file blob specified as input for this workflow task run.",
                "type": "string",
                "linkTo": "FileBlob"
            }
        },
        "outputs": {
            "title": "Output file blobs",
            "description": "File blobs that were produced as output of this workflow task run.",
            "type": "array",
            "default": [],
            "uniqueItems": true,
            "items": {
                "title": "File blob",
                "description": "A particular file blob produced as an output of this workflow task run.",
                "type": "string",
                "linkTo": "FileBlob"
            }
        }
    },
    "facets": {
        "status": {
            "title": "Status"
        }
    },
    "changelog": "/profiles/changelogs/task_run.md"
}