{
    "description": "Schema for reporting the specific calculation of an analysis_step",
    "id": "/profiles/analysis_step_run.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "required": ["analysis_step", "workflow_run", "status"],
    "additionalProperties": false,
    "identifyingProperties": ["uuid", "dx_job_id"],
    "mixinProperties": [
        { "$ref": "mixins.json#/schema_version" },
        { "$ref": "mixins.json#/aliases" },
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/submitted"}
    ],
    "properties": {
        "schema_version": {
            "default": "1"
        },
        "analysis_step": {
            "title": "Analysis Step",
            "description": "Reference to template step in pipeline",
            "type": "string",
            "linkTo": "analysis_step"
        },
        "workflow_run": {
            "title": "Analysis Step",
            "description": "Reference to template step in pipeline",
            "type": "string",
            "linkTo": "analysis_step"
        },
        "dx_job_id": {
            "title": "DNA Nexus job id",
            "description": "Identifier in the DNA Nexus system",
            "type:": "string"
        },
        "dx_app_json": {
            "title": "DNA Nexus applet json",
            "description": "dxapp JSON for the applet (wrapper code) corresponding to the step",
            "type": "object"
        },
        "started_running": {
            "title": "Start time",
            "description": "When the workflow was started",
            "type": "string",
            "format": "date-time"
        },
        "finished_running": {
            "title": "Finish time",
            "description": "When the workflow ended",
            "type": "string",
            "format": "date-time"
        },
        "status": {
            "title": "Status",
            "type": "string",
            "default": "waiting",
            "enum" : [
                "waiting",
                "running",
                "finished",
                "error",
                "virtual"
            ]
        }
    }
}
