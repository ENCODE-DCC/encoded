{
    "title": "ENCODE Antibody Characterization",
    "id": "/profiles/antibody_characterization.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "description": "Schema for submitting antibody characterization data",
    "comment": "Describes the characterization of an antibody lot submitted for review",
    "type": "object",
    "required": [ "award", "lab", "characterizes", "target" ],
    "additionalProperties": false,
    "mixinProperties": [
        { "$ref": "mixins.json#/schema_version" },
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/attachment" },
        { "$ref": "mixins.json#/attribution" },
        { "$ref": "mixins.json#/submitted" },
        { "$ref": "characterization.json#/properties" }
    ],
    "properties": {
        "schema_version": {
            "default": "1"
        },
        "characterizes": {
            "title": "Antibody characterized",
            "linkTo": "antibody_lot"
        },
        "target": {
            "description": "Target for which the antibody characterization applies.",
            "comment": "Programmatic submitters can supply a target uuid, or uniqe Target label-Organism name pair",
            "title": "Antibody target",
            "type": "string",
            "linkTo": "target"
        }
    }
}
