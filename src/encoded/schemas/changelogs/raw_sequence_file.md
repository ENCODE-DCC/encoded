## Changelog for raw_sequence_file.json

### Schema version 2
* *award* is calculated through *dataset*, instead of submitted
* *derived_from* is now a string specifying a single **SequencingRun**, instead of an array
