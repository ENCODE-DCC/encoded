{
    "title": "Organism",
    "id": "/profiles/organism.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "version": "0.01",
    "description": "Schema for submitting an organism type to ENCODE",
    "type": "object",
    "required": ["name", "taxon_id"],
    "additionalProperties": false,
    "mixinProperties": [
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/attribution" },
        { "$ref": "mixins.json#/submitted" }
    ],
    "properties": {
        "schema-version": {
            "description": "The version of JSON schema files used in the generation of the example",
            "comment":  "Must match the specific schemas' `version` property",
            "type": "string",
            "pattern": "^[0-9]\\.[0-9][0-9]$"
        },
        "name": {
            "title": "Organism name",
            "description": "A short unique name for the organism, like 'mouse'",
            "type": "string",
            "pattern": "^[a-z\\-]+$"
        },
        "scientific_name": {
            "title": "Scientific name",
            "description": "The scientific name (Genus Species) for the organism",
            "default": "",
            "type": "string"
        },
        "taxon_id": {
            "title": "Taxon ID",
            "description": "The NCBI taxon ID for the organism",
            "format": "^[0-9]+$",
            "type": "string"
        }
    }
}