{
    "title": "ENCODE Antibody Validation",
    "id": "/profiles/antibody_approval.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "version": "0.01",
    "description": "Schema for submitting antibody validation data",
    "type": "object",
    "required": [ "award", "lab", "antibody", "status", "target" ],
    "additionalProperties": false,
    "mixinProperties": [
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/submitted" }
    ],
    "properties": {
        "schema-version": {
            "description": "The version of JSON schema files used in the generation of the example",
            "comment":  "Must match the specific schemas' `version` property",
            "type": "string",
            "pattern": "^[0-9]\\.[0-9][0-9]$"
        },
        "antibody": {
            "description": "The specific antibody for which the validation image applies.",
            "comment": "Automatic submitters can supply an ENCODE antibody accession or uuid",
            "title": "Antibody to be Validated",
            "type": "string",
            "linkTo": "antibody_lot"
        },
        "target": {
            "description": "Target for which the antibody validation applies.",
            "comment": "Automatic submitters can supply a target uuid, or name/species pair",
            "title": "Antibody Target",
            "type": "string",
            "linkTo": "target"
        },
        "validations": {
            "default": [],
            "description": "List of validation images",
            "title": "Validations",
            "items": {
                "description": "Validation image used for approval",
                "comment": "Automatic submitters can supply a validation UUID or document/image name.",
                "title": "Validation image (or document)",
                "type": "string",
                "linkTo": "antibody_validation"
            },
            "type": "array"
        },
        "status": {
            "default": "INCOMPLETE",
            "description": "Approval status for the Antibody-Target-Validation triple",
            "comment": "This might not be appropriate here.",
            "title": "Approval Status",
            "type": "string",
            "enum" : [
                "APPROVED",
                "SUBMITTED",
                "UNSUBMITTED",
                "FAILED",
                "INCOMPLETE"
            ]
        }
    }
}