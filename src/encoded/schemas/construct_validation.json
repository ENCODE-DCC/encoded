{
    "title": "ENCODE Construct Validation",
    "id": "/profiles/construct_validation.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "version": "0.01",
    "description": "Schema for submitting construct validation data",
    "type": "object",
    "required": [ "award", "lab", "construct" ],
    "additionalProperties": false,
    "mixinProperties": [
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/attachment" },
        { "$ref": "mixins.json#/attribution" },
        { "$ref": "mixins.json#/submitted" },
        { "$ref": "validation.json#/properties" }
    ],
    "properties": {
        "construct": {
            "description": "The specific construct for which the validation image applies.",
            "comment": "Automatic submitters can supply a construct uuid",
            "title": "Construct to be Validated",
            "type": "string",
            "linkTo": "construct"
        }
    }
}
